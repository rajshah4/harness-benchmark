# Freight Control Tower Starter

The starter tracks shipments in memory and serves a small dashboard.

```bash
python -m pytest
python -m freight_tower.cli serve --port 8080
```

`create_server` binds but does not start its request loop. The benchmark asks you to preserve that behavior while adding durable multi-tenant operations.
